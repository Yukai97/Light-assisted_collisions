clear;
clc;

t = -10*pi:pi/250:10*pi;
x1 = (cos(2*t).^2).*sin(t);
y1 = (sin(2*t).^2).*cos(t);
x2 = (sin(2*t).^2).*cos(t);
y2 = (cos(2*t).^2).*sin(t);
L = length(t);

for i = 1:L
    plot3(x1(i),y1(i),t(i),'MarkerSize',2,'Color);
    plot3(x2(i),y2(i),t(i),'MarkerSize',2);
    drawnow
end